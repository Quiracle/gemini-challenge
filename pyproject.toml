[project]
name = "gemini-challenge"
version = "0.1.0"
description = "Lean NBA ingestion pipeline for the Gemini data engineering challenge"
requires-python = ">=3.12"
dependencies = [
    "duckdb==1.4.2",
    "requests==2.32.5",
]

[tool.uv]
package = false
